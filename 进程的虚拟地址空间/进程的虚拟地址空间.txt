代码编译之后生成指令，存放在.text段


.text      代码编译之后生成指令
.rodata "Hello World"
.data    初始化不为0的全局变量，和初始化不为零的静态变量
.bss      没有初始化或者初始化为0的全局变量和静态变量(编译器会在程序运行时候将.bss段全部初始化为0)


int a = 10;
编译之后，生成的是指令，存放在.text段中，他在运行执行的时候，在栈上找4个字节的空间存放


use space 3G  kernal space 1G
每一个进程的用户空间是私有的，内核空间是共享的
匿名管道通信